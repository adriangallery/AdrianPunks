out/
cache/
lib/
out_onchain.svg
broadcast/

# throwaway local fixture from script/gen_test_config.py — never commit (avoids
# re-baking a stale test set; the real curator config is published for provenance)
data/test-config.json
